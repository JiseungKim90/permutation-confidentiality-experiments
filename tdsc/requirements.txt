# The claim-producing historical versions are recorded separately.
# Select the official CPU wheel so a default Linux installation does not pull
# CUDA packages that this artifact neither uses nor tests.
--extra-index-url https://download.pytorch.org/whl/cpu
numpy==1.24.4
torch==2.10.0+cpu
